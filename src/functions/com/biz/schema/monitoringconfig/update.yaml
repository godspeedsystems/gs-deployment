summary: Update MonitoringConfig
tasks:
  - id: schema_monitoringconfig_update
    fn: datasource.schema.MonitoringConfig.update
    args:
      where:
        id: <% inputs.params.id %>
      data: <% inputs.body %>
