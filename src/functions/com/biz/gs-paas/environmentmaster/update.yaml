summary: Update EnvironmentMaster
tasks:
  - id: gs-paas_environmentmaster_update
    fn: datasource.gs-paas.EnvironmentMaster.update
    args:
      where:
        id: <% parseInt(inputs.params.id) %>
      data: <% inputs.body %>
