# ONE
http.get./schema/provider/{id}:
  summary: Fetch Provider
  description: Fetch Provider from database
  fn: com.biz.schema.provider.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

# CREATE
http.post./schema/provider:
  summary: Create a new Provider
  description: Create Provider from database
  fn: com.biz.schema.provider.create
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/schema/Provider'
  responses:
    '201':
      content:
        application/json:
          schema:
            type: object

# UPDATE
http.put./schema/provider/{id}:
  summary: Update a Provider
  description: Update Provider from database
  fn: com.biz.schema.provider.update
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/schema/Provider'
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '204':
      content:
        application/json:
          schema:
            type: object

# DELETE
http.delete./schema/provider/{id}:
  summary: Delete a Provider
  description: Delete Provider from database
  fn: com.biz.schema.provider.delete
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '202':
      content:
        application/json:
          schema:
            type: object

# SEARCH
http.post./schema/provider/search:
  summary: Fetch multiple Provider
  description: Fetch multiple Provider from database
  fn: com.biz.schema.provider.search
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array

