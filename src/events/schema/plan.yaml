# ONE
http.get./schema/plan/{id}:
  summary: Fetch Plan
  description: Fetch Plan from database
  fn: com.biz.schema.plan.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object

# CREATE
http.post./schema/plan:
  summary: Create a new Plan
  description: Create Plan from database
  fn: com.biz.schema.plan.create
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/schema/Plan'
  responses:
    '201':
      content:
        application/json:
          schema:
            type: object

# UPDATE
http.put./schema/plan/{id}:
  summary: Update a Plan
  description: Update Plan from database
  fn: com.biz.schema.plan.update
  body:
    content:
      application/json:
        schema:
          $ref: '#/definitions/schema/Plan'
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '204':
      content:
        application/json:
          schema:
            type: object

# DELETE
http.delete./schema/plan/{id}:
  summary: Delete a Plan
  description: Delete Plan from database
  fn: com.biz.schema.plan.delete
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    '202':
      content:
        application/json:
          schema:
            type: object

# SEARCH
http.post./schema/plan/search:
  summary: Fetch multiple Plan
  description: Fetch multiple Plan from database
  fn: com.biz.schema.plan.search
  body:
    content:
      application/json:
        schema:
          type: object
  responses:
    '200':
      content:
        application/json:
          schema:
            type: array

